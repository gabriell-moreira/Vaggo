spring:
  application:
    name: Vaggo

  datasource:
    url: jdbc:postgresql://localhost:5431/VaggoDB
    username: vaggo
    password: vaggo123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration